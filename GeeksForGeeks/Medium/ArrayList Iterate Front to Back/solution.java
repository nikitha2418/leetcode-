// User function Template for Java
class Solution {

    public static void iterateArrayList(Iterator it) {
        // Your code here
        while (it.hasNext()) {
        System.out.print(it.next() + " ");
        }
        // Just print the elements, don't provide new line
    }
}